import React from 'react';
import Base from '../Base';

const Gallery = () => {
    return (
        <div>
            <Base>
                <h1>Gallery</h1>
            </Base>
        </div>
    );
}

export default Gallery;